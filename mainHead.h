#ifndef MAINHEAD_H_INCLUDED
#define MAINHEAD_H_INCLUDED

#include <conio.h> reda
#include <iostream>
#include <string>
#include <stdio.h>
#include <windows.h>

#define first(L) L.first
#define last(L) L.last
#define next(P) P->next
#define prev(P) P->prev
#define info(P) P->info
#define toP(P) P->toP
#define toC(P) P->toC

using namespace std;

#endif // MAINHEAD_H_INCLUDED
